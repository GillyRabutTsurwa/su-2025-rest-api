<%- include ("./partials/header") %>
    <% plugins.forEach((currentPlugin)=> { %>
        <h3>
            <%= currentPlugin.name %>
        </h3>
    <% }) %>

<% if (user) { %>
    User: <h4> <%= user.username %> </h4>
    <a href="/logout">Logout</a>
<% } %>